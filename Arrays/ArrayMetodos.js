array.push(algo)  // adiciona no final do array;
array.pop()  // tira do final;
array.shift()  // tira do começo;
array.unshift(algo)  // adiciona no começo;

array.splice(0, 4)// tira do valor 0 até o valor 4;

array.splice(0, 4, "posso adicionar", "quantos quiser até 4");

array.splice(2, 0, "do index 2", "deleta 0", "e adiciona as strings");
